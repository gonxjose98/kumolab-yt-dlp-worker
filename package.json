{
  "name": "kumolab-yt-dlp-worker",
  "version": "1.0.0",
  "private": true,
  "description": "YouTube downloader worker — bypasses YouTube's AWS Lambda IP block by running on Render's clean IP range. Vercel calls /download with a YouTube URL, gets back the muxed MP4 bytes.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
